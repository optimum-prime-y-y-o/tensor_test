## Тестовое задание на разработчика в тестировании
Выполнено три сценария тестирования. Логи сохраняются в **./logs/tests.log** (будет создано автоматически)
#### Установка зависимостей
`pip install -r requirements.txt`

#### Запуск тестов
`pytest -v`